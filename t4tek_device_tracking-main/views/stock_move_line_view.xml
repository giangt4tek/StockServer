<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="stock_t4tek_move_line_action" model="ir.actions.act_window">
        <field name="name">Lịch sử di chuyển</field>
        <field name="res_model">stock.move.line</field>
        <field name="view_mode">list,kanban,pivot,form</field>
        <field name="domain">[('picking_id.is_device_tracking','=',True)]</field>
        <field name="view_id" ref="stock.view_move_line_tree" />
        <field name="context">{'search_default_done': 1, 'create': 0, 'pivot_measures':
            ['quantity_product_uom', '__count__']}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_empty_folder">
                There's no product move yet
            </p>
            <p>
                This menu gives you the full traceability of inventory operations on a specific
                product.
                You can filter on the product to see all the past movements for the product.
            </p>
        </field>
    </record>
</odoo>